# Windows GPU requirements
# TF 2.10 is the last version with native Windows GPU support (no WSL2 needed).
# TF 2.11+ requires WSL2 for GPU on Windows.
#
# CUDA pre-requisites (install before pip packages):
#   CUDA Toolkit 11.2  — https://developer.nvidia.com/cuda-11.2.0-download-archive
#   cuDNN 8.1 for CUDA 11.2 — https://developer.nvidia.com/rdp/cudnn-download
#
# OR use conda which handles CUDA automatically:
#   conda create -n capstone python=3.10
#   conda activate capstone
#   conda install -c conda-forge cudatoolkit=11.2 cudnn=8.1
#   pip install -r requirements-windows-gpu.txt
#
tensorflow-gpu==2.10.*
deap>=1.4
numpy>=1.24,<2.0
scikit-learn>=1.3
faiss-cpu>=1.7
pmlb>=1.0
tqdm>=4.65
